package wasp.types;

/**
 * Interface for subsections of NameSection.
 * 
 * valid types:
 *  * ModuleName
 *  * FunctionNames
 *  * LocalNames
 */
interface NameSubSection extends Marshaller {
    
}